[build-system]
requires = ["setuptools >= 40.6.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.ruff]
fix = true
line-length = 88  # Default line length for black

[tool.ruff.lint]
select = ["I", "E", "F", "W"]